# Naman Sahu — Portfolio (Vite + React)

This is a production-ready single-page portfolio built with Vite, React, Tailwind CSS and Framer Motion.
Prefilled with details from Naman Sahu's resume.

## Features
- Responsive single-page site (mobile + desktop)
- Tailwind CSS styling
- Framer Motion animations
- Contact form (EmailJS optional) + mailto fallback
- AI "Generate Summary" button UI (serverless proxy required to call OpenAI)
- Production-ready build (`dist/`)

## Quick start
1. Install deps:
   ```bash
   npm install
